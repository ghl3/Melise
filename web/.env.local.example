# Copy to .env.local for local dev; set the same vars in Vercel for prod.
MODEL_SERVER_URL=http://localhost:8000
# Must match SERVE_TOKEN on the worker (leave unset if the worker has none).
MODEL_SERVER_TOKEN=
